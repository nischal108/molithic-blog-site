<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4">Todo List</h1>
        <form action="/todo/add" method="POST" class="mb-8">
            <div class="mb-4">
                <label for="title" class="block text-gray-800 font-bold mb-2">Title:</label>
                <input type="text" id="title" name="title" required
                    class="w-full px-3 py-2 border rounded-md bg-white focus:outline-none focus:border-blue-500">
            </div>
            <div class="mb-4">
                <label for="status" class="block text-gray-800 font-bold mb-2">Status:</label>
                <select id="status" name="status"
                    class="w-full px-3 py-2 border rounded-md bg-white focus:outline-none focus:border-blue-500">
                    <option value="Pending" class="bg-white text-gray-800">Pending</option>
                    <option value="Completed" class="bg-white text-gray-800">Completed</option>
                    <option value="In Progress" class="bg-white text-gray-800">In Progress</option>
                    <option value="Cancelled" class="bg-white text-gray-800">Cancelled</option>
                </select>
            </div>
            <div class="mb-4">
                <label for="description" class="block text-gray-800 font-bold mb-2">Description:</label>
                <textarea id="description" name="description" required
                    class="w-full px-3 py-2 border rounded-md bg-white focus:outline-none focus:border-blue-500"></textarea>
            </div>
            <button type="submit"
                class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 transition duration-200">Add
                Todo</button>
        </form>

        <h2 class="text-2xl font-bold mb-4">All Todos:</h2>
        <table class="w-full border-collapse border border-gray-300">
            <thead>
                <tr>
                    <th class="px-4 py-2 bg-gray-200 text-gray-800">Title</th>
                    <th class="px-4 py-2 bg-gray-200 text-gray-800">Status</th>
                    <th class="px-4 py-2 bg-gray-200 text-gray-800">Description</th>
                    <th class="px-4 py-2 bg-gray-200 text-gray-800">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% todos.forEach((todo, index)=> { %>
                    <tr>
                        <td class="px-4 py-2 border border-gray-300">
                            <%= todo.title %>
                        </td>
                        <td class="px-4 py-2 border border-gray-300">
                            <%= todo.status %>
                        </td>
                        <td class="px-4 py-2 border border-gray-300">
                            <%= todo.description %>
                        </td>
                        <td class="px-4 py-2 border border-gray-300">
                            <form action="/todo/delete/<%= todo.id %>" method="post" class="inline">
                                <button type="submit"
                                    class="bg-red-500 text-white px-2 py-1 rounded-md hover:bg-red-600 transition duration-200">Delete</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
    </div>
</body>

</html>